const initialState = {
	email: 'thenickjantz@gmail.com',
	name: 'Nick Jantz',
	level: 'admin'
}

const user = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state
	}
}

export default user